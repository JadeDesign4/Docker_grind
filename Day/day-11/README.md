	
	As you noticed, I no longer add the app folders I'm using into the working directory (day-1.. e.t.c).
 That Is because I now specify the path to the application in the ".env" file.
	I also added new syntax in the docker-compose.yml:-
		The "logging" section. Since that's what I just recently learnt

